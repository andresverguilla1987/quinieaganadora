# Quiniela-AI (MVP)
Repo generated by ChatGPT — contains backend (FastAPI) and frontend (Vite React).
Backend: `backend/`
Frontend: `frontend/` (ready to deploy to Netlify)

Steps:
- Backend: python venv, pip install -r requirements.txt, run uvicorn app.main:app --reload --port 8000
- Frontend: npm install, npm run dev (or build and deploy to Netlify)